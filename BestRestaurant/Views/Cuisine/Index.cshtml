@{
  Layout = "_Layout";
}

@using BestRestaurant.Models;

<h1>Cuisines</h1>

@if (@Model.Count == 0)
{
  <h3>No cuisines have been added yet!</h3>
} 

@foreach (Cuisine cuisine in Model)
{
  <li>@cuisine.RestaurantName</li>
}